"use client";

import { CatalogBrowser } from "@/components/store/catalog-browser";

/** @deprecated Prefer CatalogBrowser directly — kept for existing imports. */
export function ProductGrid() {
  return <CatalogBrowser />;
}
